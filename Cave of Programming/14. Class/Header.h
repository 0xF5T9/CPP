#pragma once

class program {
public:	// Chỉ định Access specifier, public cho phép nó có thể được truy cập và chỉnh sửa từ bên ngoài class
	void Welcome();	// Constructor 
	int TinhTong(int x, int y);	// Constructor 
	double SoPI();	// Constructor 
	std::string Code();	// Constructor 
};